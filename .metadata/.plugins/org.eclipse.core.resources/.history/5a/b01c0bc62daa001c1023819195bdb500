package com.taller1.demo;

import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.junit.jupiter.api.Assertions.assertTrue;
import static org.mockito.Mockito.when;

import java.math.BigDecimal;
import java.util.Optional;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import com.taller1.demo.model.prod.Location;
import com.taller1.demo.model.prod.Productcosthistory;
import com.taller1.demo.repositories.LocationRepository;
import com.taller1.demo.repositories.ProductcosthistoryRepository;
import com.taller1.demo.services.LocationServiceImp;
import com.taller1.demo.services.ProductcosthistoryService;

class ProductcosthistoryTest {

	@Mock
	private ProductcosthistoryRepository pchRepository;

	@InjectMocks
	private ProductcosthistoryService pchService;

	private Productcosthistory pch0;
	private Optional<Productcosthistory> pch0op;
	
	private Productcosthistory pch1;


	@BeforeEach
	void setUp1() {
		MockitoAnnotations.openMocks(this);
		
		pch0 = new Productcosthistory();
		pch0.setLocationid(1);
		pch0.setName("locat");
		pch0.setAvailability(new BigDecimal(2));
		pch0.setCostrate(new BigDecimal(1));
	}

	@Test
	void testThatSaveAnProduct() {
		when(locationRepository.save(pch0)).thenReturn(pch0);
		assertNotNull(ls.saveLocation(pch0));
	}
	
	@Test
	void testConstraints() {
		when(locationRepository.save(pch0)).thenReturn(pch0);
		Location testLoc = ls.saveLocation(pch0);
		assertTrue(testLoc.getName().length() >= 5, "ConstraintName");
		assertTrue(testLoc.getAvailability().intValue() >= 1 && testLoc.getAvailability().intValue() <= 10, "ConstraintAvailability");
		assertTrue(testLoc.getCostrate().intValue() == 0 || testLoc.getCostrate().intValue() == 1, "ConstraintCostRate");
	}
	
	@BeforeEach
	void setUp2() {
		MockitoAnnotations.openMocks(this);
		
		pch0op = Optional.of(pch0);
		
		pch1 = new Location();
		pch1.setLocationid(1);
		pch1.setName("admin");
		pch1.setAvailability(new BigDecimal(3));
		pch1.setCostrate(new BigDecimal(4));
	}
	
	@Test
	void testThatEditAnProduct() {
		when(locationRepository.findById(1)).thenReturn(pch0op);
		when(locationRepository.save(pch1)).thenReturn(pch1);
		assertNotNull(ls.editLocation(pch1, 1));
	}
}
